# TASKS

## Completed

- Audit current SEO working tree.
- Fix high-priority technical SEO risks.
- Run `npm test`.
- Run `npm run lint`.
- Run `npm run build`.
- Pull latest `main` and verify the working tree before second-round SEO work.
- Harden localized canonical/hreflang/noindex/robots coverage.
- Add SEO regression tests for locale routing, sitemap alternates, robots, and content clustering.
- Push `main` to GitHub at commit `14a5345`.

## Next

- Confirm Vercel deployment status for commit `14a5345`.
- If deployment passes, inspect production `https://labelhelper.com/zh/etsy-shipping-label-size`, `/robots.txt`, and `/sitemap.xml` for the same rendered SEO tags verified locally by tests/build.

## Future SEO backlog

- Add default OG/Twitter image if social sharing quality becomes a priority.
- Use Search Console data to identify cannibalization or thin long-tail pages.
- Consider translated slugs and fuller body localization for Spanish/Chinese if international SEO becomes a priority.
- Continue expanding only task-specific content clusters that route users back to a tool, checklist, or conversion action.
